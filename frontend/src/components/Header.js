import React from "react";

const Header = () => {
  return (
    <header
      style={{
        display: "flex",
        alignItems: "center",
        marginTop: "25px",
        justifyContent: "center",
      }}
    >
      <h1>Home Dashboard!</h1>
    </header>
  );
};

export default Header;
